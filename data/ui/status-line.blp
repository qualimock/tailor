using Gtk 4.0;

template $TailorStatusLine : Box {
	notify::state => $state_to_status ();

	Label {
		halign: start;
		hexpand: true;
		ellipsize: end;

		label: bind template.title;
	}

	Label {
		styles [ "heading" ]

		halign: end;
		label: bind template.status;
	}
}
